# Ticket-Ordering-System (C++)
This project simulates a ticket-based queue management system for service lines (e.g., bill payment or subscriptions).  he demo creates two separate queues with different prefixes and average serve times, then performs ticket issuance, serving, and info printing operations.
